const HomePage = () => {
  return (
    <p>
      This Vending Machine system allows users with a "seller" role to add,
      update or remove products, while users with a "buyer" role can deposit
      coins into the machine and make purchases. The Vending Machine only accept
      5, 10, 20, 50 and 100 cent coins.
    </p>
  );
};

export default HomePage;
