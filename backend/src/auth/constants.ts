export const jwtConstants = {
  // In a production system we must protect this key using appropriate measures
  // such as a secrets vault, environment variable, or configuration service.
  secret: 'mySecretKey',
};
